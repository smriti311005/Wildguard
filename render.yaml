services:
  - type: web
    name: wildguard-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn cloud_backend.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
